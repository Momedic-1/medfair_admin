# Copy to .env.local for local overrides.
# Development (npm run dev) uses .env.development automatically.
VITE_API_URL=http://localhost:8081

# Production builds (npm run build) use .env.production.
# Vercel: also set VITE_API_URL in Project -> Settings -> Environment Variables.
# VITE_API_URL=https://backend-h3k6.onrender.com